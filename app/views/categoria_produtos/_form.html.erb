<%= form_for(@categoria_produto) do |f| %>
  <% if @categoria_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoria_produto.errors.count, "error") %> prohibited this categoria_produto from being saved:</h2>

      <ul>
      <% @categoria_produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :nome_categoria %><br>
    <%= f.text_field :nome_categoria %>
  </div>
  
<div class="row">
	<div class="col s6 m6 l6 offset-l10-m10-s10">
		<button class="btn waves-effect waves-light" type="submit" name="action">
			Submit <i class="material-icons right">send</i>
		</button>
	</div>
</div>
<% end %>
